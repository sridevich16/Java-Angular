server:
  port: 9097
spring:
  application:
    name: Account-service
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true