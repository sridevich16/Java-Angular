server:
  port: 9098
spring:
  application:
    name: Savings-service
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true